[project]
name = "pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "nbconvert>=7.16.6",
]

[dependency-groups]
dev = [
    "ipykernel>=7.1.0",
]
